bin/
obj/
.vs/
.idea/
*.user
*.suo
artifacts/
TestResults/
_server/
_nupkgs/
_stage/

# The browser tools/Capture downloads, and the profile it drives.
_capture/
.mcp.json

# What the health check captured while walking the sources.
health/

# The release package, built by CI and never committed. A zip in the tree is a
# release nobody can trace to a commit.
*.zip
