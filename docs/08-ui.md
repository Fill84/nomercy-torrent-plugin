# The user interface

## Two rules

**Every number is real.** A count that is not known is not drawn as nought; it says what is missing.
0.3.4 showed "0 downloads" while two were running.

**Everything updates live.** No page polls. The pipeline publishes to the activity journal, the
journal pushes a snapshot over `IPluginHubContext.PushAsync("torrent-downloader:changed", …)` at most
once every 250 ms, and every page renders from that snapshot.

## Dashboard — the landing page at `/`

```
┌─ Torrent Downloader ─────────────────────────────────────────────┐
│ ● Running   search cycle · 3 of 24 episodes · started 4 min ago  │
│                                              [ Run ] [ Stop ]    │
├──────────────────────────────────────────────────────────────────┤
│  Harvest    ████████████████████  4/4 feeds      239 names       │
│  Names      ████████████░░░░░░░░  18/24 episodes  6 asked live   │
│  Find       ██████████░░░░░░░░░░  11/17 indexers  4 answering    │
│  Grab       3 taken                                              │
│  Download   2 running · 1 finished · 0 failed                    │
│  Dispatch   1 encode queued                                      │
├──────────────────────────────────────────────────────────────────┤
│  Now                                                             │
│   Silo S03E06        20 names → asking 1337x, TPB, TorrentGalaxy │
│   Lioness S03E01     waiting on TorrentBay (rate limit, 8s)      │
│   Sugar S02E02       chose Sugar.2024.S02E02.1080p.WEB.H264-NTb  │
├──────────────────────────────────────────────────────────────────┤
│  Sources     ✔ 14 answering   ⏸ 2 rate limited   ✖ 1 refused     │
│              eztvx.to 45s · predb.me 12s · torrentbay ✖ challenge│
├──────────────────────────────────────────────────────────────────┤
│  Downloads                                                       │
│   Silo.S03E06.1080p.WEB.H264-CAKES ███████░░ 68% 12MB/s 3m ⇅1.2  │
│   Lioness.S03E01.1080p.WEB.H264-NTb ██░░░░░░ 21%  4MB/s 14m      │
└──────────────────────────────────────────────────────────────────┘
```

| Section | Answers | Components |
| --- | --- | --- |
| Status bar | is it working, on what, since when | `Container`, `Badge`, `Text`, `Button` |
| Stages | how far along, where it is stuck | `Progress` per stage with counts |
| Now | which episodes are in flight this second and what each is waiting on | `Table` |
| Sources | which sites are healthy, waiting, or broken | `Row` of `Badge`, `Table` on click |
| Downloads | what is transferring, with ratio while seeding | `Table` with `Progress`, `Bytes`, `Rate`, `Duration` |

**Run** starts a full cycle, answers immediately, and disables itself while a cycle runs. The next
scheduled pass of each cadence stands down once.

**Stop** cancels the running cycle. Transfers already handed to the torrent client keep going —
stopping a search is not stopping a download.

When nothing is running the status bar says when the last cycle ran, what it found, and when the
next is due. `EmptyState` is for a plugin with nothing configured, not for an idle one.

Anything the pipeline does that cannot appear in **Now** is a stage that has not been instrumented,
and an uninstrumented stage does not ship.

## Detail pages

| Page | For |
| --- | --- |
| Shows | every show in the tv and anime libraries, what is missing from it, its media type, last arrival |
| Queue | what is being looked for, in the order it will be asked, with attempts and last tried; separately what is waiting to air, with the date |
| Downloads | in flight and finished today: progress, rate, peers, seeds, ratio, destination folder |
| History | grabbed, skipped and why, failed and why, dispatched to which library, allowed by hand |
| Skipped | releases the profile or the blacklist refused, with the reason, and the control to allow one anyway |
| Sources | per source: what it last answered, how long it took, its refusal in its own words, when it is next askable |
| Settings | folders, cadences, quality, torrent client, own indexers, private trackers, dry run, Run |

Each page has a test that renders it from a seeded store and asserts the numbers.

## Actions

Every one of these is an endpoint and a control on the page named:

| Action | Page | Does |
| --- | --- | --- |
| `RunNow` | Dashboard, Settings | start a full cycle in the background |
| `StopRun` | Dashboard | cancel the running cycle; transfers keep going |
| `SearchNow` | Queue | search one episode immediately, outside the cadence |
| `PauseDownload` | Downloads | pause a transfer, keeping its pieces |
| `ResumeDownload` | Downloads | resume a paused transfer |
| `CancelDownload` | Downloads | stop it, remove it, and return the episode to missing |
| `AddTorrent` | Downloads | add a magnet or `.torrent` by hand, outside the search chain |
| `AllowRelease` | Skipped | allow a release the profile or blacklist refused, and grab it |
| `SaveSettings` | Settings | save the settings, with validation |
| `AddIndexer` / `SaveIndexer` / `RemoveIndexer` | Settings | the owner's own indexers |
| `AddPrivateTracker` / `SavePrivateTracker` / `RemovePrivateTracker` | Settings | the owner's own private trackers |

`AddTorrent` still runs the finished file through staging and the encode dispatch, because a torrent
added by hand is an episode like any other. When its name cannot be matched to an episode, it is
downloaded and staged, and the dispatch says which file it could not place.

`AllowRelease` records an `allowed` history entry naming the release and the reason it had been
refused, so a page never silently contradicts an earlier decision.

**Where each control sits.** A table cell holds a value and not a button, and a row carries one
action, so the layout follows from that rather than from taste. A row *is* the control where there
is one thing to do with it: the Queue's rows search that episode now, and the Skipped page's rows
allow that release. Where there are several — Downloads, with pause, resume and cancel — the numbers
stay in the table and a strip of buttons per download sits under it, each naming its release, or an
owner with four downloads cannot tell which Cancel is which. Pause and resume are one button that
says which it is, because a torrent is one or the other. Cancel carries a `PluginConfirmation` and is
marked destructive: it deletes what has been downloaded so far, and leaving that to the client would
have one asking and another not. `AddTorrent` is a `Form` with one field, because a button with
nothing to type into is an action nobody can use.

## Settings

- **Folders** — incomplete and intake. Validated on save; a folder that cannot be written says so on
  the page. A warning when the two are on different volumes, because every completion then pays a
  full-file copy.
- **Cadences** — four cron expressions; an invalid one is refused with the reason.
- **Quality** — resolution rung, codec, require codec tag, minimum seeders, allow season packs,
  season-pack threshold, forbidden terms, English only, include specials, maximum search attempts.
- **Torrent client** — listen port, port mapping, maximum download and upload rate, seed ratio, seed
  hours, stall minutes, metadata timeout, maximum concurrent downloads, encryption.
- **Own indexers** — name, address with `{query}`, kind, priority, minimum interval, API key,
  enabled. The API key is write-only.
- **Private trackers** — host and announce URL, the URL carrying `{passkey}` where the passkey goes.
  The passkey is write-only: the page renders that one is set, never the value, and is handed only
  the names of the secrets that exist so it has no value it could render.
- **Shipped sources** — a list with a switch each, writing `DisabledDefaultSources`. Addresses are
  shown but not editable.
- **Dry run** — the pipeline runs to a decision per episode and hands nothing to the torrent client.
- **Run now**.

## Components

From `NoMercy.Plugins.Abstractions`, nothing outside it:

- `PluginComponentType`: `Container`, `Text`, `Image`, `List`, `Row`, `Grid`, `Card`, `Detail`,
  `Button`, `Form`, `WebView`, `EmptyState`, `Spinner`, `Table`, `Progress`, `Badge`
- `PluginTableCellType`: `Text`, `Progress`, `Badge`, `Bytes`, `Rate`, `Duration`
- `PluginBadgeVariant`: `Neutral`, `Info`, `Success`, `Warning`, `Danger`
- `PluginActionType`: `PlayMedia`, `Enqueue`, `Navigate`, `CallPlugin`, `OpenWebView`, `RefreshView`
- `PluginLayout`: `Standard`, `ListDetail`, `Grid`, `Form`

Dashboard is `Standard`; Shows and Queue are `ListDetail`; Settings is `Form`.

## Navigation

Two mounts in `plugin.json`, matching `IUiPlugin.NavEntries` exactly (a test asserts it):

- `library` → `/`
- `settings` → `/settings`
